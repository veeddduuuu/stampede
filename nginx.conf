events {}

http {
    upstream stampede-backend {
        least_conn;
        server api1:8080;
        server api2:8080;
        server api3:8080;
    }

    server {
        listen 80;
        location / {
            proxy_pass http://stampede-backend;
        }
    }
}